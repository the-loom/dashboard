<legend>
  Listado de emblemas
  <%= link_to 'Nuevo emblema', new_badge_url, class: 'btn btn-primary pull-right' %>
</legend>

<table class="table table-hover">
  <thead>
  <tr>
    <th>Imagen</th>
    <th>Emblema</th>
    <th>Descripción</th>
    <th>Otorgamientos</th>
  </tr>
  </thead>
  <tbody>
  <% @badges.each do |badge| %>
    <tr>
      <td>
        <img class="loom-badge" src="<%= image_url("badges/#{badge.slug}.png") %>"></td>
      </td>
      <td>
        <%= link_to badge.name, badge_details_url(badge.id) %>
      </td>
      <td>
        <%= badge.description %>
      </td>
      <td>
        <%= badge.earnings.size %> veces
      </td>
    </tr>
  <% end %>
  </tbody>
</table>
