<% nav_add 'Aula', '#' %>
<% nav_add 'Clases', '#' %>
<%= nav_render %>

<div class="row">
  <%= link_to 'Nueva clase', new_lecture_path, class: 'btn btn-primary pull-right' %>
</div>

<% if @lectures.empty? %>

  <div class="alert alert-info">
    <p>Aún no hay ninguna clase</p>
  </div>

<% else %>

  <table class="table table-hover">
    <thead>
    <tr>
      <th>Resumen</th>
      <th>Fecha</th>
    </tr>
    </thead>
    <tbody>
    <% @lectures.each do |lecture| %>
      <tr>
        <td>
          <%= lecture.summary %>
        </td>
        <td>
          <%= l lecture.date.in_time_zone('Buenos Aires') %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>

<% end %>
