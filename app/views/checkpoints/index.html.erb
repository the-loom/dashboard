<legend>
  Listado de entregas
  <%= link_to 'Nueva entrega', new_checkpoint_url, class: 'btn btn-primary pull-right' %>
</legend>

<table class="table table-hover">
  <thead>
  <tr>
    <th>Nombre</th>
    <th>Enlace</th>
    <th>Fecha límite</th>
  </tr>
  </thead>
  <tbody>
  <% @checkpoints.each do |checkpoint| %>
    <tr>
      <td>
        <%= link_to checkpoint.name, checkpoint_details_url(checkpoint.id) %>
      </td>
      <td>
        <%= link_to checkpoint.link do %>
          <%= checkpoint.link %> <i class="fa fa-external-link"></i>
        <% end %>
      </td>
      <td>
        <%= l checkpoint.due_date.in_time_zone('Buenos Aires') %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>
